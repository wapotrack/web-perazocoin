'use client'
import React from 'react';

import { Box, Flex, HStack, IconButton, Link, Drawer, DrawerOverlay, DrawerContent, DrawerCloseButton, DrawerBody, useColorModeValue, Image, Heading } from '@chakra-ui/react';
import { HamburgerIcon, UpDownIcon, LinkIcon, InfoIcon } from '@chakra-ui/icons';

const SideBar = () => {

  return (
    <Flex marginTop='100px'>
        <div />
            America
        <div />
    </Flex>
  );
};

export default SideBar;
